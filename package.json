{
  "devDependencies": {
    "style-dictionary": "^5.3.2"
  },
  "dependencies": {
    "@tokens-studio/sd-transforms": "^2.0.3"
  },
  "type": "module",
  "scripts": {
    "preclean": "rm -rf build && mkdir -p build/css build/flutter",
    "clean": "rm -rf build",
    "tokens": "npm run preclean && node build-tokens.js"
  }
}
